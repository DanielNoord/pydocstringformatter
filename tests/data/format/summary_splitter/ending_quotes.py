def func():
    """We should keep the position of the quotes
    as is.
    """


def func():
    """We should keep the position of the quotes
    as is."""


def func():
    """We should keep the position of the quotes

    as is.
    """


def func():
    """We should keep the position of the quotes

    as is."""
